# golem-data-sync

Java 17 / Spring Boot backend and Next.js frontend for MySQL batch synchronization through engine profiles: real local Apache SeaTunnel Zeta 2.3.13, a local Spark mock, and an unconfigured Flink extension point.

Never commit credentials, `.env`, `.runtime/`, `data/`, logs, or downloaded SeaTunnel binaries.
